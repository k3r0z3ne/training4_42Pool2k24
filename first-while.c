#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    int valide;

    valide = 0;
    while (valide <11)
    {
        printf("Vous pouvez passer\n");
        valide++;
    }
    return (0);
}